---
description: 依序結算所有角色、販售低階裝備、最佳化配裝並重新掛機
---

請依 `/chars` 的即時角色清單順序，處理目前所有既有角色（最多 9 名）。先讀取 `playing-bear-of-bears` 與 `bears-equipment-strategy` skills，並遵守其查詢、完整性、操作確認及停止規則。

這是一個範圍明確的全角色批次任務。我明確授權在每名角色上執行以下流程：

1. 切換至該角色並確認角色身分；若正在掛機，執行 `/stopidle`，確認獎勵已結算。
2. 走安全路線前往可使用商店的安全地點。每次只移動一個房間並確認結果；避開所有 BOSS 房間。
3. 執行且只執行 `/sellall 稀有`。先確認遊戲端預覽僅涵蓋稀有及更低稀有度裝備，並保留史詩、傳說、裝備中、鎖定物品、各部位最強一件及消耗品，再確認販售；完成後以 `/inventory all` 驗證結果。此授權適用於本批次的每名角色，不必逐次詢問；禁止其他 `/sellall` 變體。
4. 依 `bears-equipment-strategy` 最佳化配裝；未另指定策略時使用 `ATK／DEF／INT／AGI = 1／1／1／1`。只換上經完整背包快照、資格與特殊效果檢查後確認為嚴格提升的裝備，並逐件驗證。
5. 依角色最新等級、能力、位置、公開地圖及已驗證經驗，選擇能提高實際 `EXP／分鐘`、且不在 BOSS 房間的適合掛機地點。將移動與恢復時間納入考量；若缺乏足夠證據證明遠行更好，選擇鄰近且安全可達的合適野外房間。到達後確認角色狀態，再執行 `/idle` 並確認已開始掛機。
6. 完成整批或因停止條件中止後，更新文件：在 `docs/CHARS.md` 記錄最新角色清單、等級、目前角色、金幣與掛機 checkpoint；在 `docs/LOG.md` 記錄逐角販售、換裝、路線、掛機結果及 blocker；只有產生可重用的策略或限制時才更新 `docs/NOTES.md`。所有狀態須附來源訊息編號；位置與掛機狀態若可能變動，須標示為最後觀測，不得把未確認結果寫成已完成。

本 prompt 明確授權完成上述流程所需的角色切換、安全移動、必要恢復、`/stopidle`、`/sellall 稀有` 預覽確認、嚴格提升換裝及 `/idle`；不授權購買、其他販售或銷毀、交易、公開聊天、PvP、建立／刪除／進階角色。

逐一處理其餘角色。單一角色遇到可安全略過的配裝或路線 blocker 時，記錄原因，仍盡可能讓該角色在已確認安全且非 BOSS 的合適地點掛機，再繼續下一名；若發生 Telegram rate limit、死亡、角色身分不明或操作結果不明，停止整批，不重送結果不明的操作。更新文件後，逐一回報每名角色的販售、換裝、位置、掛機結果及任何未完成事項。
