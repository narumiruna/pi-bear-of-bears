# 九角滿級衝刺

## 目標

在至少 4–6 小時的長掛機累積後，結算並檢查目前九名既有角色，讓九角全員達到遊戲目前上限總 Lv170，並確認九角最後都在掛機。

目前待追趕角色的歷史 checkpoint 為：

- `なるみ_狂戰熊`：二轉 Lv69 95%
- `なるみ_鐵壁熊`：二轉 Lv67
- `なるみ_元素熊`：二轉 Lv65

以上只供安排順序；角色、職業、等級、位置及掛機狀態一律以本輪開始後的 `/chars` 與 `/status` 即時回覆為準。

指定 BOSS 裝、BOSS 圖鑑及 concept-art 改為本輪 checkpoint，不是本目標的完成門檻。結算時仍須記錄新掉落與新解鎖，但不因隨機掉落尚未發生而阻擋九角滿級驗收。

## 執行流程

開始前閱讀 `playing-bear-of-bears` 與 `bears-equipment-strategy` skills，並依 `/chars` 顯示順序處理最多九名既有角色。

1. 查詢 `/chars`，確認角色順序、即時等級、目前角色與掛機狀態。
2. 逐角切換；若正在掛機，執行 `/stopidle` 並確認結算，記錄 EXP、金幣、掉落、圖鑑與 concept-art 新增項目。
3. 查詢 `/status`；對尚未達總 Lv170 的角色取得完整 `/inventory all`，依職業權重執行嚴格提升配裝：
   - 只換上資格、屬性與特殊效果均已確認，且分數嚴格高於現裝的裝備。
   - 每次換裝後重新取得完整背包，不沿用舊編號。
   - 每名角色最多嘗試 6 次換裝；無嚴格提升時維持現裝。
4. 已達總 Lv170 的角色不為等級進行額外手動戰鬥；記錄即時狀態後重新掛機。
5. 未達總 Lv170 的角色移至可安全到達、非 BOSS、普通怪密度較高且能穩定取勝的掛機起點。Lv155 以上優先使用白霧雪坡等已驗證冰原高密度區；若不可達，使用公開地圖選擇安全替代路線。
6. 執行 `/idle` 並確認成功後才切換下一名角色。
7. 全部處理後切回原本角色，再以最新 `/chars` 驗證九角等級與掛機狀態。

輪值的目的是維持九角持續取得進度；不要求角色等級同步成長，也不因短期隨機掉落缺失而重複結算。

## 授權與限制

本目標授權：

- 切換九名既有角色；
- `/stopidle`、`/idle`、安全移動與必要的免費恢復；
- `/chars`、`/status`、完整背包、裝備詳情、圖鑑及 gallery 查詢；
- 經完整資料驗證的嚴格提升換裝；
- 公開地圖與 BOSS 圖鑑的唯讀查詢。

本目標不授權：

- 購買、製作、進化或任何未提供明確金幣／材料預算的支出；
- 任一形式的販售、銷毀、交易或轉移；
- 使用藥水或其他消耗品；
- 主動 BOSS 戰、PvP、公開聊天；
- 建立、刪除、改名、進階角色或其他帳號變更；
- terminal `sleep`、`goal_wait`、背景迴圈或輪詢累積進度。

Telegram rate limit、死亡、角色身分不明、非預期費用或操作結果不明時停止整批；不重送結果不明的操作。單一角色遇到可安全略過的 blocker 時，記錄原因、盡可能恢復掛機，再繼續下一名。

## 紀錄

本輪結束或因停止條件中止後：

- 更新 `docs/CHARS.md`：九角即時等級、職業、位置、目前角色及掛機 checkpoint。
- 更新 `docs/LOG.md`：逐角結算、換裝、路線、新掉落、新圖鑑項目、重新掛機結果及 blocker。
- 只有產生可重用策略時才更新 `docs/NOTES.md` 或相關 skill reference。
- 所有狀態附遊戲訊息編號；位置與掛機狀態若可能變動，標示為最後觀測。

## 完成條件

只有同時符合以下條件才算完成：

- 最新 `/chars` 證明九名既有角色皆為總 Lv170。
- 本輪由 `/status` 驗證三名歷史待追趕角色已達總 Lv170；若即時 `/chars` 顯示待追趕角色不同，則改驗證所有本輪升至上限的角色。
- 最新 `/chars` 證明九角皆有 🐾 掛機標記。
- 本輪結算發現的指定 BOSS 裝、BOSS 圖鑑及 concept-art 進度均已記錄，但不要求全收集。
- `docs/CHARS.md` 與 `docs/LOG.md` 已更新為本輪最新 checkpoint。
