# 九角色共同目標

## 核心目標

- 讓 9 個角色全部抵達 Lv170。
- 每個角色依 `docs/CHARS.md` 記錄的目標，於 Lv100 後進行對應二轉；二轉前不得擅自選擇其他分支。
- 以「同時培養、分階段達成」取代要求 9 個角色同步等級：每次集中處理一個角色，其餘角色維持安全的遊戲內掛機或停留在安全區。

## 執行策略

- 優先讓目前等級最低的角色追上，其次優先處理距離 Lv100 最近、即將能二轉的角色。
- 每個角色建立獨立進度 checkpoint，記錄於 `docs/CHARS.md`；不要把其他角色的等級、裝備或 EXP 套用到目前角色。
- 先使用遊戲內掛機累積各角色 EXP，再依即時狀態選擇必要的手動練等；不得建立 Agent 端背景刷怪迴圈。
- 為每個角色依職業選擇裝備與練等方式；背包不完整時，不宣稱全背包最佳化，也不猜測隱藏物品。
- 裝備、道具、金幣與二轉費用分角色記錄；除非取得額外授權，不購買、銷毀、交易或轉移物品。
- 角色切換前後重新確認 `/chars` 與 `/status`；每次最多讓一個角色作為目前操作角色，避免把操作結果記到錯誤角色。

## 輪流遊玩機制

採用「九角色輪值機制」，以角色名稱作為唯一識別：

1. 從 `docs/CHARS.md` 讀取九個角色，選出目前等級最低的角色。
2. 若有兩個以上角色並列最低等級，從這些角色中隨機挑選一個；隨機結果與選擇依據寫入 `docs/LOG.md`。
3. 操作選出的角色一次；完成後重新讀取所有角色的最新等級，再依相同規則選擇下一個角色。不可預先固定順序或因偏好跳過最低等級角色。若前一角色已安全掛機，切換後讓其繼續掛機。
4. 切換前確認 `/chars`；切換後立即查詢 `/status`，確認角色名稱、等級、位置、HP／MP 與掛機狀態。
5. 為該角色執行一次有界遊玩工作階段：優先在安全區啟動遊戲內掛機；若需要手動練等，僅進行已確認安全且有明確停止條件的操作。
6. 若已成功啟動掛機，立即交還該角色並切換至下一個候選角色；不得在同一角色上繼續手動操作或重複啟動掛機。
7. 切換前或交還角色時，重新查詢 `/status`；將等級、EXP、資源、位置、裝備與操作結果寫入 `docs/CHARS.md`／`docs/LOG.md`。
8. 已在安全掛機的角色僅查詢 `/idlestatus` 與必要狀態，不重複啟動掛機；若掛機未結算，不因輪值切換而猜測收益已入帳。
9. 角色達 Lv170 後標記完成，不再列入最低等級候選，只做必要驗證；其餘角色持續依最新狀態選擇。
10. 任一角色遇到死亡、資源不足、結果不明、資料不完整或非預期費用，立即停止該角色，記錄阻擋原因，重新選擇目前最低等級角色；不得重送不確定操作。

輪值的目的，是讓九個角色都持續獲得進度，而不是要求九個角色保持相同等級或同時進行手動戰鬥。

## 里程碑

- [x] 9 個角色皆至少 Lv1，並完成 `docs/CHARS.md` 的名稱、目前職業、目標進階職業與等級記錄。
- [ ] 9 個角色皆達 Lv100，逐一確認二轉條件與費用後，再依目標完成二轉。
- [ ] 9 個角色皆達 Lv170，並為每個角色保存最後 `/status` checkpoint。
- [ ] 持續將已驗證的練等技巧與裝備決策記錄至 `docs/NOTES.md`。
- [ ] 將重要操作與狀態變化記錄至 `docs/LOG.md`；最多 1000 行，超過時刪除最舊的 500 行。
