# 全部角色持續掛機

## 目標

- **最高優先：讓帳號內全部角色都保持遊戲內掛機，持續取得進度。**
- 目前上限為 9 名角色；角色清單、職業與等級一律以最新 `/chars` 為準，不使用文件中的歷史數值代替即時狀態。
- 輪值目的為讓所有角色持續進步，不要求等級相同，也不啟動手動戰鬥循環。
- 本檔是執行規格，不自行啟動 Goal mode；實際操作仍須有當次使用者對全角色掛機的明確授權。

## 執行流程

1. 閱讀 `docs/CHARS.md`、`skills/playing-bear-of-bears/SKILL.md` 與近期 `bears_history`，排除尚未釐清的操作結果。
2. 使用 `start_idle` 處理最多 9 名角色；讓工具依即時 `/chars` 切換角色、結算既有掛機、避開 BOSS 房移動並重新啟動 `/idle`。
3. 路線優先依角色等級選擇已知狩獵區；偏好區不可由非 BOSS 路線抵達時，依序回退至較低階的固定狩獵區，或留在目前可掛機的非 BOSS 房。
4. 公開地圖的 `monsterCount` 缺少代表未知，不等於零；固定狩獵區只有在怪物數量明確為 0、安全區或 BOSS 房時才排除。
5. 成功後以工具結果確認每名角色為 `started` 或 `already_running`；必要時以最新 `/chars` 驗證全員皆有掛機標記。

## 失敗時的改進流程

- `start_idle` 失敗時，立即停止批次，不重送結果不明的移動、切換或掛機指令。
- 在 `docs/LOG.md` 記錄失敗原因、已完成／未完成角色、最後確認的目前角色與所需恢復步驟；無法確認者明確標示未知。
- 找出可重現原因，優先改善 `extensions/auto-idle.ts`、`src/auto-idle.ts`、相關測試或 `skills/playing-bear-of-bears/` 指引，不以 hooks、scripts 或背景迴圈繞過遊戲限制。
- 修改 extension 後執行相關 Vitest 與 `npm run build`；確認通過後必須重新載入 extension，才可再次使用新版 `start_idle`。
- 若阻擋來自公開地圖暫時缺資料，採保守且可驗證的回退路線；若目前位置、出口或操作結果不明，先重新觀測，不猜測路線。

## 安全與授權界線

- 全程避開公開地圖標記的 BOSS 房；每次移動都必須符合已確認出口，Telegram 回覆不符即停止。
- 掛機授權不包含購買、販售、銷毀、交易／轉移、公開聊天、PvP、建立／刪除角色或帳號變更。
- 不使用 `goal_wait`。需要等待時只能使用單次、前景且有明確期限的 terminal `sleep`，醒來後重新讀取遊戲狀態。
- 操作結果不明時依 skill 最多補讀兩次 `bears_history`；仍無法確認就停止，不重送。

## 完成條件

- 最新且可驗證的結果涵蓋全部即時角色，且每名角色均已成功啟動掛機或已在掛機。
- 部分完成、路線阻擋、未知結果或僅修改文件／程式碼均不算完成。
